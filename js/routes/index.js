import {GROUP_LIST, GROUP_DETAILS, GROUP_CONTACT_DETAILS, EDIT_CONTACT_DETAILS} from './constants';

const routes = {
  
};

routes[GROUP_LIST] = {
    title: 'Contact Groups'
};

routes[GROUP_DETAILS] = {
    title: 'Contact Group'
};

routes[GROUP_CONTACT_DETAILS] = {
    title: 'View Contact Details'
};

routes[EDIT_CONTACT_DETAILS] = {
    title: 'Edit Contact Details'
};

export default routes;